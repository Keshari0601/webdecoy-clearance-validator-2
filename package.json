{
  "name": "webdecoy-clearance-validator-2",
  "private": true,
  "description": "WebDecoy Cloudflare Worker — clearance enforcement and non-JavaScript crawler detection",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.51.0"
  }
}
